# Folder: frontend/member-h5/src/utils

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Utilities contain pure formatting, information sanitizing, history/receipt shaping, PNG export, and browser-compatibility helpers. / 工具目录包含纯格式化、信息清洗、历史/凭证整形、PNG 导出与浏览器兼容辅助函数。
They must stay deterministic at their API boundary except explicit random key generation. / 除明确的随机键生成外，它们的 API 边界必须保持确定。
Business decisions belong in stores or backend services. / 业务决策属于 Store 或后端服务。

## Files
- `money.ts`: frontend/member-h5/src/utils - cent-to-yuan formatter / 分转元格式化器
- `idempotency.ts`: frontend/member-h5/src/utils - LAN-safe UUID idempotency key generator / 局域网安全的 UUID 幂等键生成器
- `information.ts`: frontend/member-h5/src/utils - information category labels and DOMPurify HTML sanitizing / 信息分类标签与 DOMPurify HTML 清洗
- `pageTitle.ts`: frontend/member-h5/src/utils - browser document title helper / 浏览器页面标题辅助工具
- `demoBootstrap.ts`: frontend/member-h5/src/utils - development-only visual QA fixture / 仅开发环境视觉验证夹具
- `registrationHistory.ts`: frontend/member-h5/src/utils - read-only standard, multi-group, and progressive group registration history shaper / 只读普通、多羽与递进组报名历史整形器
- `registrationReceipt.ts`: frontend/member-h5/src/utils - print receipt identity, project summaries, details, and filename shaping / 打印凭证身份、项目汇总、明细与文件名整形
- `registrationReceiptExport.ts`: frontend/member-h5/src/utils - adaptive html2canvas rendering, download, mobile detection, system file sharing, and WeChat original-image data URLs / 自适应 html2canvas 渲染、下载、移动端检测、系统文件分享与微信原图数据 URL
