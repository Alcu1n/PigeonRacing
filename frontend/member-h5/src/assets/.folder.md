# Folder: frontend/member-h5/src/assets

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Assets store bundled H5 UI images and icons. / Assets 存放会被 H5 打包的 UI 图片与图标。
Components import assets through Vite instead of absolute local paths. / 组件通过 Vite 导入资产，而不是使用本机绝对路径。
Icons must stay small and deterministic for mobile matrix cells. / 图标必须保持小体积且适配移动端矩阵单元格。

## Files
- `star.svg`: frontend/member-h5/src/assets - selected single-project matrix star icon / 单羽项目矩阵选中态星标图标
