# Folder: frontend/member-h5/src/components

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Components render registration controls and shared member profile/session/return actions. / 组件渲染报名控件与共享会员档案/会话/返回动作。
They dispatch user actions but avoid route-level data loading. / 它们派发用户动作，但避免承担路由级数据加载。
Full-width matrix, sticky group controls with group counts, compact amount, profile, logout, and return UI must stay readable on mobile. / 全宽矩阵、带成组数的固定组队控件、紧凑金额、个人信息、退出与返回 UI 必须在手机端保持可读。

## Files
- `AmountBar.vue`: frontend/member-h5/src/components - ultra-compact fixed bottom amount and submit bar / 超紧凑固定底部金额与提交栏
- `MemberLogoutButton.vue`: frontend/member-h5/src/components - compact authenticated logout action / 紧凑已登录退出动作
- `MemberTopActions.vue`: frontend/member-h5/src/components - compact profile, logout, and optional race-list return action group / 紧凑个人信息、退出与可选返回赛事列表动作组
- `MultiGroupBuilder.vue`: frontend/member-h5/src/components - sticky multi-pigeon group builder with selected-project group count / 带当前项目成组数的固定多羽组合构建器
- `SelectedDetail.vue`: frontend/member-h5/src/components - selected registration detail preview / 已选报名明细预览
- `SingleMatrix.vue`: frontend/member-h5/src/components - full-width compact single-pigeon matrix with row select-all / 带行全选的全宽紧凑单羽矩阵
