# Folder: frontend/member-h5/src/components

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Components render registration controls, progressive stage group controls, receipt images, and shared member profile/session/return actions. / 组件渲染报名控件、递进阶段组控件、报名凭证图片与共享会员档案/会话/返回动作。
They dispatch user actions and keep receipt detail loading scoped to explicit download actions. / 它们派发用户动作，并仅在明确下载动作中加载凭证明细。
Full-width matrix, sticky group controls with group counts, progressive stage group matrix, compact amount, profile, logout, and return UI must stay readable on mobile. / 全宽矩阵、带成组数的固定组队控件、递进阶段组矩阵、紧凑金额、个人信息、退出与返回 UI 必须在手机端保持可读。

## Files
- `AmountBar.vue`: frontend/member-h5/src/components - ultra-compact fixed bottom amount and submit bar / 超紧凑固定底部金额与提交栏
- `MemberLogoutButton.vue`: frontend/member-h5/src/components - compact authenticated logout action / 紧凑已登录退出动作
- `MemberTopActions.vue`: frontend/member-h5/src/components - compact profile, logout, and optional race-list return action group / 紧凑个人信息、退出与可选返回赛事列表动作组
- `MultiGroupBuilder.vue`: frontend/member-h5/src/components - sticky multi-pigeon group builder with selected-project group count / 带当前项目成组数的固定多羽组合构建器
- `ProgressiveStageMatrix.vue`: frontend/member-h5/src/components - current progressive stage whole-group eligibility matrix / 当前递进阶段整组资格勾选矩阵
- `RegistrationReceipt.vue`: frontend/member-h5/src/components - fixed-width print-style registration receipt renderer / 固定宽度的打印风报名凭证渲染器
- `RegistrationReceiptDownload.vue`: frontend/member-h5/src/components - shared desktop PNG download, mobile preview/share, and WeChat original-image saving workflow / 共享电脑 PNG 下载、手机预览分享与微信原图保存流程
- `SelectedDetail.vue`: frontend/member-h5/src/components - selected standard, multi-group, and progressive group registration detail preview / 普通、多羽与递进组已选报名明细预览
- `SingleMatrix.vue`: frontend/member-h5/src/components - full-width compact single-pigeon matrix with row select-all and SVG star selected markers / 带行全选与 SVG 星标选中态的全宽紧凑单羽矩阵
