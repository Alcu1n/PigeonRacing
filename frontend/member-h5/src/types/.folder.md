# Folder: frontend/member-h5/src/types

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Types describe backend JSON contracts consumed by the H5 app, including member password policy, public information, progressive group categories, and registration history. / 类型描述 H5 应用消费的后端 JSON 契约，包括会员密码策略、公开信息、递进组类别与报名历史。
They prevent UI code from inventing fields not supplied by API. / 它们防止 UI 代码发明 API 未提供的字段。
Cent amounts remain integer values. / 分金额保持整数值。

## Files
- `domain.ts`: frontend/member-h5/src/types - race, project, progressive category group, pigeon, member profile, public information, registration/history contracts, and localized status helpers / 赛事、项目、递进类别组、足环、会员档案、公开信息、报名/历史契约与本地化状态辅助函数
