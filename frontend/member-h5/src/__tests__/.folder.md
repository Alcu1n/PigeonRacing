# Folder: frontend/member-h5/src/__tests__

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Tests verify local registration behavior, progressive selections, database restore priority, member auth, public information helpers, published race details, receipt exports, history shaping, and browser compatibility without backend traffic. / 测试验证无后端流量的本地报名行为、递进选择、数据库恢复优先级、会员鉴权、公开信息辅助函数、已发布赛事明细、凭证导出、历史整形与浏览器兼容性。
They focus on store rules, route guards, and submit helpers that views depend on. / 它们聚焦视图依赖的 Store 规则、路由守卫与提交辅助函数。
Stale drafts, database snapshots, amount math, progressive stages, row select-all, repeat usage, unique groups, selected group counts, history, UUID fallback, and forced password changes must stay covered. / 过期草稿、数据库快照、金额计算、递进阶段、行全选、重复使用、唯一组合、已成组数、历史、UUID 回退与强制改密必须保持覆盖。

## Files
- `authStore.test.ts`: frontend/member-h5/src/__tests__ - auth login, profile restore, password update, and runtime reset tests / 登录、档案恢复、改密与运行时重置测试
- `registrationStore.test.ts`: frontend/member-h5/src/__tests__ - registration store database restore, draft priority, progressive selection, and unique group tests / 报名 Store 数据库恢复、草稿优先级、递进选择与唯一组合测试
- `idempotency.test.ts`: frontend/member-h5/src/__tests__ - UUID fallback tests for non-secure LAN browsers / 非安全局域网浏览器 UUID 回退测试
- `information.test.ts`: frontend/member-h5/src/__tests__ - information labels and rich HTML sanitizing tests / 信息标签与富文本清洗测试
- `publishedRaceDetails.test.ts`: frontend/member-h5/src/__tests__ - race list detail button and published detail tab/search rendering tests / 赛事列表明细按钮与已发布明细标签/搜索渲染测试
- `registrationHistory.test.ts`: frontend/member-h5/src/__tests__ - history matrix, receipt summary, and guard tests / 历史矩阵、凭证汇总与守卫测试
- `registrationReceipt.test.ts`: frontend/member-h5/src/__tests__ - receipt filename, bounded scale/retry, download cleanup, and file-share compatibility tests / 凭证文件名、受限倍率与重试、下载清理及文件分享兼容测试
- `registrationReceiptEntrypoints.test.ts`: frontend/member-h5/src/__tests__ - receipt entrypoint ordering, list action isolation, and mobile WeChat fallback tests / 凭证入口顺序、列表动作隔离与移动微信降级测试
- `routerGuard.test.ts`: frontend/member-h5/src/__tests__ - forced password-change and public information route guard tests / 强制改密与公开信息路由守卫测试
