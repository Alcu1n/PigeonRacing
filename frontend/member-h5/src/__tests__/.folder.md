# Folder: frontend/member-h5/src/__tests__

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Tests verify local registration behavior, database restore priority, member auth, history shaping, and browser compatibility without backend traffic. / 测试验证无后端流量的本地报名行为、数据库恢复优先级、会员鉴权、历史整形与浏览器兼容性。
They focus on store rules, route guards, and submit helpers that views depend on. / 它们聚焦视图依赖的 Store 规则、路由守卫与提交辅助函数。
Stale drafts, database snapshots, amount math, row select-all, repeat usage, unique groups, selected group counts, history, UUID fallback, and forced password changes must stay covered. / 过期草稿、数据库快照、金额计算、行全选、重复使用、唯一组合、已成组数、历史、UUID 回退与强制改密必须保持覆盖。

## Files
- `authStore.test.ts`: frontend/member-h5/src/__tests__ - auth login, profile restore, password update, and runtime reset tests / 登录、档案恢复、改密与运行时重置测试
- `registrationStore.test.ts`: frontend/member-h5/src/__tests__ - registration store database restore, draft priority, and unique group tests / 报名 Store 数据库恢复、草稿优先级与唯一组合测试
- `idempotency.test.ts`: frontend/member-h5/src/__tests__ - UUID fallback tests for non-secure LAN browsers / 非安全局域网浏览器 UUID 回退测试
- `registrationHistory.test.ts`: frontend/member-h5/src/__tests__ - history matrix and guard tests / 历史矩阵与守卫测试
- `routerGuard.test.ts`: frontend/member-h5/src/__tests__ - forced password-change route guard tests / 强制改密路由守卫测试
