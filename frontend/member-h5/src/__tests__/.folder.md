# Folder: frontend/member-h5/src/__tests__

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Tests verify local registration behavior without backend traffic. / 测试验证无后端流量的本地报名行为。
They focus on store rules that views depend on. / 它们聚焦视图依赖的 Store 规则。
Stale drafts, amount math, row select-all, and repeat group rules must stay covered. / 过期草稿、金额计算、行全选与重复组队规则必须保持覆盖。

## Files
- `registrationStore.test.ts`: frontend/member-h5/src/__tests__ - registration store behavior tests / 报名 Store 行为测试
