# Folder: frontend/member-h5/src/stores

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Stores own member session and registration state machines. / Store 负责会员会话与报名状态机。
Registration math and submit payload derivation live here, not in views. / 报名计算与提交数据派生在这里，不在视图中。
Draft restore must reject stale `config_version`. / 草稿恢复必须拒绝过期 `config_version`。

## Files
- `auth.ts`: frontend/member-h5/src/stores - member login/logout state / 会员登录退出状态
- `registration.ts`: frontend/member-h5/src/stores - local registration matrix and group state / 本地报名矩阵与组合状态
