# Folder: frontend/member-h5/src/stores

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Stores own member session, profile/password, and registration state machines. / Store 负责会员会话、档案改密与报名状态机。
Registration math, profile restore, password updates, and submit payload derivation live here, not in ad hoc views. / 报名计算、档案恢复、密码更新与提交数据派生在这里，不在临时视图逻辑中。
Draft restore must reject stale `config_version`. / 草稿恢复必须拒绝过期 `config_version`。

## Files
- `auth.ts`: frontend/member-h5/src/stores - member login/logout, profile restore, and password update state / 会员登录退出、档案恢复与改密状态
- `registration.ts`: frontend/member-h5/src/stores - local registration matrix, row select-all, repeat-aware unique group state / 本地报名矩阵、行全选与支持重复规则的唯一组合状态
