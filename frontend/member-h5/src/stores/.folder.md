# Folder: frontend/member-h5/src/stores

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Stores own member session, profile/password, registration runtime reset, and registration state machines. / Store 负责会员会话、档案改密、报名运行时重置与报名状态机。
Registration math, profile restore, password updates, database restore, progressive stage group selection, and submit payload derivation live here. / 报名计算、档案恢复、密码更新、数据库恢复、递进阶段组选中与提交数据派生在这里。
Draft restore must reject stale `config_version` and older-than-database drafts. / 草稿恢复必须拒绝过期 `config_version` 与早于数据库提交的草稿。

## Files
- `auth.ts`: frontend/member-h5/src/stores - member login/logout, profile restore, password update, and registration runtime reset / 会员登录退出、档案恢复、改密与报名运行时重置
- `registration.ts`: frontend/member-h5/src/stores - database-prioritized standard matrix, progressive group matrix, row select-all, drafts, and repeat-aware unique group state / 数据库优先普通矩阵、递进组矩阵、行全选、草稿与支持重复规则的唯一组合状态
