# Folder: frontend/member-h5/src/api

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

API modules own HTTP transport and Sanctum cookie setup. / API 模块负责 HTTP 传输与 Sanctum Cookie 设置。
They must not calculate registration totals or mutate local registration state. / 它们不得计算报名金额或修改本地报名状态。
All member requests use same-origin credentials. / 所有会员请求都使用同源凭据。

## Files
- `client.ts`: frontend/member-h5/src/api - Axios client and CSRF bootstrap / Axios 客户端与 CSRF 初始化
