# Folder: frontend/member-h5/src

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Vue source is split by API, state, route, view, component, asset, type, utility, and mobile shell boundaries. / Vue 源码按 API、状态、路由、视图、组件、资产、类型、工具与移动端外壳边界拆分。
Stores own registration math, member profile state, and password update boundaries. / Store 拥有报名计算、会员档案状态与改密边界。
Submit payloads and history views must use API snapshots, LAN-safe UUID keys, and respect forced password changes. / 提交数据与历史视图必须使用 API 快照、局域网安全 UUID 键，并遵守强制改密规则。

## Files
- `main.ts`: src - Vue application bootstrap with zoom gesture blocking / 带缩放手势拦截的 Vue 应用启动
- `App.vue`: src - route shell / 路由外壳
- `style.css`: src - locked-zoom mobile UI, centered logo login/footer, localized status pills, highlighted race list, aligned registration actions, compact amount bar, profile cards, history detail, SVG matrix star sizing, readable compact matrix layer, and sticky group styles / 锁定缩放的移动端 UI、居中 Logo 登录页/页脚、本地化状态标签、突出赛事列表、对齐的报名页动作、紧凑金额栏、档案卡片、历史详情、SVG 矩阵星标尺寸、可读紧凑矩阵层级与固定组队样式
- `api/`: src/api - backend HTTP client / 后端 HTTP 客户端
- `assets/`: src/assets - bundled UI icons and images / 打包进前端的 UI 图标与图片
- `components/`: src/components - reusable registration UI / 可复用报名 UI
- `router/`: src/router - route definitions / 路由定义
- `stores/`: src/stores - Pinia local state / Pinia 本地状态
- `types/`: src/types - TypeScript data contracts / TypeScript 数据契约
- `utils/`: src/utils - formatting and browser-compatibility helpers / 格式化与浏览器兼容工具
- `views/`: src/views - route-level screens / 路由级页面
