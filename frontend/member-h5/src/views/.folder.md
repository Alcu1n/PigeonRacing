# Folder: frontend/member-h5/src/views

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Views own route-level data loading and screen composition. / 视图负责路由级数据加载与页面组合。
They call API only at allowed workflow boundaries and show boundary-specific errors. / 它们只在允许的流程边界调用 API 并显示边界对应错误。
Local registration clicks must remain store-only. / 本地报名点击必须只进入 Store。

## Files
- `LoginView.vue`: frontend/member-h5/src/views - member login screen / 会员登录页
- `RaceListView.vue`: frontend/member-h5/src/views - visible race list / 可见赛事列表
- `RegistrationView.vue`: frontend/member-h5/src/views - core registration workflow / 核心报名流程
- `ResultView.vue`: frontend/member-h5/src/views - registration success result / 报名成功结果
