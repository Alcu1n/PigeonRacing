# Folder: frontend/member-h5/src/views

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Views own route-level data loading, centered logo login composition, compact workflows, profile/password flow, and shared session actions. / 视图负责路由级数据加载、居中 Logo 登录页组合、紧凑流程、档案改密流程与共享会话动作。
They call API only at allowed workflow boundaries and show boundary-specific errors. / 它们只在允许的流程边界调用 API 并显示边界对应错误。
Local registration clicks must remain store-only. / 本地报名点击必须只进入 Store。

## Files
- `LoginView.vue`: frontend/member-h5/src/views - member login screen with uploaded logo, footer copyright, and first-login password prompt / 带上传 Logo、页脚版权与首次改密提示的会员登录页
- `ProfileView.vue`: frontend/member-h5/src/views - member profile, pigeon list, and password update screen / 会员档案、足环列表与改密页面
- `RaceListView.vue`: frontend/member-h5/src/views - visible race list with logout / 带退出的可见赛事列表
- `RegistrationView.vue`: frontend/member-h5/src/views - ultra-compact core registration workflow / 超紧凑核心报名流程
- `ResultView.vue`: frontend/member-h5/src/views - registration success result with logout / 带退出的报名成功结果
