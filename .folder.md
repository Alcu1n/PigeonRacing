# Folder: /

> Trigger: When this repository's top-level structure/responsibilities/file list changes, update this document.
> 触发条件:当本仓库的顶层结构/职责/文件列表变化时，更新此文档。

This root coordinates product docs, backend, frontend, and deployment maps. / 根目录协调产品文档、后端、前端与部署地图。
Backend owns trusted business state; frontend owns fast local member interaction. / 后端拥有可信业务状态；前端拥有快速本地会员交互。
Every structural change must keep README and child `.folder.md` files aligned. / 任何结构变化必须保持 README 与子级 `.folder.md` 对齐。

## Files
- `README.md`: root - canonical project map / 项目强制正典地图
- `.folder.md`: root - local folder map / 当前目录局部地图
- `.gitignore`: root - generated dependency, cache, and runtime symlink ignore rules / 生成依赖、缓存与运行时软链接忽略规则
- `docker-compose.yml`: root - service orchestration with separate public storage mount / 带独立公开存储挂载的服务编排
- `赛鸽赛事报名系统产品说明文档.md`: root - product requirements source / 产品需求来源
