# Folder: backend/resources/views/filament/resources/member-resource

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Member resource views render custom member admin workflows. / 会员资源视图渲染自定义会员后台流程。
They depend on `MemberResource` page classes. / 它们依赖 `MemberResource` 页面类。
Import validation remains in `MemberImportService`. / 导入校验仍归 `MemberImportService` 所有。

## Files
- `pages/`: backend/resources/views/filament/resources/member-resource/pages - member resource page views / 会员资源页面视图
