# Folder: backend/resources/views/filament/resources/member-resource/pages

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Page views render member admin Livewire pages. / 页面视图渲染会员后台 Livewire 页面。
They bind to public properties and methods on matching page classes. / 它们绑定到对应页面类的公开属性与方法。
Preview tables must display service-produced row state without recomputing it. / 预览表只能展示服务生成的行状态，不得重新计算。

## Files
- `import-members.blade.php`: backend/resources/views/filament/resources/member-resource/pages - spaced styled member Excel upload, preview, confirm, and report download UI / 间距稳定且样式统一的会员 Excel 上传、预览、确认与报告下载 UI
