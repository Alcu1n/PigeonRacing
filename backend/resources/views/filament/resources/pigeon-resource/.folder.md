# Folder: backend/resources/views/filament/resources/pigeon-resource

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Pigeon resource views render custom pigeon admin workflows. / 足环资源视图渲染自定义足环后台流程。
They depend on `PigeonResource` page classes. / 它们依赖 `PigeonResource` 页面类。
Import validation remains in `PigeonImportService`. / 导入校验仍归 `PigeonImportService` 所有。

## Files
- `pages/`: backend/resources/views/filament/resources/pigeon-resource/pages - pigeon resource page views / 足环资源页面视图
