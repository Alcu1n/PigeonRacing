# Folder: backend/resources/views/filament/resources

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Resource views hold admin screens that need custom markup. / 资源视图承载需要自定义标记的后台页面。
Resource page classes supply all mutable state. / 资源页面类提供全部可变状态。
Folder names must match their Filament resource routes. / 文件夹名称必须匹配 Filament 资源路由。

## Files
- `member-resource/`: backend/resources/views/filament/resources/member-resource - member resource views / 会员资源视图
- `pigeon-resource/`: backend/resources/views/filament/resources/pigeon-resource - pigeon resource views / 足环资源视图
