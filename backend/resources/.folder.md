# Folder: backend/resources

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Resources contain backend-rendered view assets. / Resources 存放后端渲染视图资产。
Laravel code owns state; Blade files only render admin interaction surfaces. / Laravel 代码拥有状态；Blade 文件只渲染后台交互界面。
Views must not duplicate import or export business rules. / 视图不得重复导入或导出业务规则。

## Files
- `views/`: backend/resources/views - Blade view tree / Blade 视图树
