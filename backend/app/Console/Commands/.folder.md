# Folder: backend/app/Console/Commands

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Commands are operational entry points for controlled synthetic load testing. / 命令是受控合成压测的运维入口。
Provisioning creates only marked synthetic rows; cleanup requires the exact run identifier and an explicit `--force`. / 准备命令只创建带标识的合成数据；清理必须提供准确运行标识并显式追加 `--force`。

## Files
- `ProvisionLoadTestData.php`: backend/app/Console/Commands - creates isolated synthetic members, pigeons, race, project, and JSON credentials / 创建隔离的合成会员、足环、赛事、项目与 JSON 凭据
- `CleanupLoadTestData.php`: backend/app/Console/Commands - previews or manually deletes one guarded synthetic dataset / 预览或手动删除一个受保护的合成数据集
