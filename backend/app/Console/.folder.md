# Folder: backend/app/Console

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Console contains manually invoked Artisan commands that support safe operational workflows. / Console 包含支持安全运维流程的手动 Artisan 命令。
Commands in this folder must be explicit, scoped, and safe to preview before destructive actions. / 本目录命令必须明确范围，并且破坏性操作必须支持先预览。

## Files
- `Commands/`: backend/app/Console/Commands - isolated production load-test data provision and manual cleanup commands / 隔离生产压测数据准备与手动清理命令
