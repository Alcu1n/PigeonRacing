# Folder: backend/app/Services

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Services own cross-model workflows that must not leak into controllers. / 服务负责不应泄漏到控制器的跨模型流程。
Imports own preview-confirm transactions; registration submission owns validation and duplicate-group rules. / 导入负责预览确认事务；报名提交负责校验与重复组合规则。
Bootstrap cache keys must include project config versions and be cleared after member or pigeon imports. / 初始化缓存键必须包含项目配置版本，并在会员或足环导入后清理。

## Files
- `MemberImportService.php`: backend/app/Services - member Excel row normalization, preview, and commit / 会员 Excel 行规范化、预览与写入
- `PigeonImportService.php`: backend/app/Services - Excel row normalization, preview, and commit / Excel 行规范化、预览与写入
- `RaceCacheService.php`: backend/app/Services - bootstrap/config/pigeon cache coordination and invalidation / 初始化、配置与足环缓存协调和失效
- `RegistrationRuleException.php`: backend/app/Services - API-safe rule exception / API 安全规则异常
- `RegistrationSubmissionService.php`: backend/app/Services - trusted registration validation, unique-group rules, and transaction / 可信报名校验、唯一组合规则与事务
