# Folder: backend/app/Services

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Services own cross-model workflows, shared query aggregates, progressive stage imports, admin registration edits, published race details, and view-shaped registration snapshots. / 服务负责跨模型流程、共享查询聚合、递进阶段导入、后台报名编辑、已发布赛事明细与报名快照展示整形。
Imports own server-side preview tokens, target pigeon library validation, and a shared member-account import lock; registration submission owns member and project-library validation; admin edits own manual snapshot rebuilds and progressive cascade cleanup. / 导入负责服务端预览令牌、目标足环库校验与共享会员账号导入锁；会员提交负责会员端与项目足环库校验；后台编辑负责手动快照重建与递进联动清理。
Bootstrap cache keys must include project config versions and be cleared after member, pigeon, project, category, import, or registration changes. / 初始化缓存键必须包含项目配置版本，并在会员、足环、项目、类别、导入或报名变化后清理。

## Files
- `AdminRegistrationEditService.php`: backend/app/Services - admin-only standard/progressive registration editing, totals, cascade cleanup, cache invalidation, and audit logging / 后台专用普通与递进报名编辑、金额、联动清理、缓存失效与审计记录
- `MemberImportService.php`: backend/app/Services - member Excel row normalization, preview, and commit / 会员 Excel 行规范化、预览与写入
- `MemberCredentialImportService.php`: backend/app/Services - existing-member phone/password Excel validation, password-safe preview, and revalidated commit / 已有会员手机号/密码 Excel 校验、密码安全预览与复核写入
- `PigeonImportService.php`: backend/app/Services - Excel row normalization, target-library duplicate checks, server-side preview cache, chunked lookup, and commit / Excel 行规范化、目标库重复检查、服务端预览缓存、分块查询与写入
- `ProgressiveStageImportService.php`: backend/app/Services - progressive first-stage Excel normalization, group parsing, preview token, auto-create, and confirmed baseline commit / 递进第一阶段 Excel 规范化、组解析、预览令牌、自动创建与已确认基准写入
- `PublishedRaceDetailsService.php`: backend/app/Services - race-level published detail read model for member H5 tabs / 会员端标签页使用的整场赛事已发布明细读模型
- `RaceCacheService.php`: backend/app/Services - bootstrap/config/project-library pigeon read coordination and invalidation / 初始化、配置与项目足环库读模型协调和失效
- `RegistrationDetailMatrixService.php`: backend/app/Services - ring-first registration snapshot matrix shaping for admin detail views, including progressive groups / 包含递进分区的足环优先报名快照后台详情矩阵整形
- `RegistrationRuleException.php`: backend/app/Services - API-safe rule exception / API 安全规则异常
- `RegistrationSummaryService.php`: backend/app/Services - registration amount and loft aggregate queries / 报名金额与棚数聚合查询
- `RegistrationSubmissionService.php`: backend/app/Services - trusted registration validation, project-library eligibility, short readable numbers, unique-group rules, progressive whole-group rules, and transaction / 可信报名校验、项目足环库资格、短可读编号、唯一组合规则、递进整组规则与事务
