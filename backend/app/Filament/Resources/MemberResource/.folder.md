# Folder: backend/app/Filament/Resources/MemberResource

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

This folder contains routed pages for member administration and Excel import. / 本文件夹包含会员管理与 Excel 导入的可路由页面。
It depends on `MemberResource` for forms and `MemberImportService` for import rules. / 它依赖 `MemberResource` 提供表单，并依赖 `MemberImportService` 提供导入规则。
Pages must not own member import, login, or password policy business rules. / 页面不得拥有会员导入、登录或密码策略业务规则。

## Files
- `Pages/`: backend/app/Filament/Resources/MemberResource/Pages - member resource page classes / 会员资源页面类
