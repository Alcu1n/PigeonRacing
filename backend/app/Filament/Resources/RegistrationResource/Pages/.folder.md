# Folder: backend/app/Filament/Resources/RegistrationResource/Pages

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Registration pages expose summary-backed list with manual refresh, eager-loaded matrix detail view, admin data editing, delete-all cleanup, and export actions. / 报名页面暴露支持手动刷新的带汇总列表、预加载矩阵详情、后台数据编辑、全部删除清理与导出操作。
They delegate table, overview, and matrix structure to `RegistrationResource`. / 它们把表格、概览与矩阵结构委托给 `RegistrationResource`。
They edit registration entries through `AdminRegistrationEditService`; cleanup uses `RegistrationResource` deletion actions. / 它们通过 `AdminRegistrationEditService` 编辑报名明细；清理通过 `RegistrationResource` 删除操作执行。

## Files
- `EditRegistrationData.php`: backend/app/Filament/Resources/RegistrationResource/Pages - admin registration data edit page / 后台报名数据编辑页面
- `ListRegistrations.php`: backend/app/Filament/Resources/RegistrationResource/Pages - registration list page with manual refresh, inline summary, race export action, and delete-all action / 带手动刷新、内联汇总、赛事导出操作与全部删除操作的报名列表页面
- `ViewRegistration.php`: backend/app/Filament/Resources/RegistrationResource/Pages - registration view page / 报名查看页面
