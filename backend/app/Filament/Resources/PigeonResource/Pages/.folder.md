# Folder: backend/app/Filament/Resources/PigeonResource/Pages

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Pigeon pages expose list, create, and edit routes. / 足环页面暴露列表、创建与编辑路由。
They delegate fields to `PigeonResource` and keep create-only range logic local. / 它们把字段委托给 `PigeonResource`，并把创建专用范围逻辑留在本地。
Header actions stay limited to page-local commands. / 头部操作仅限页面局部命令。

## Files
- `CreatePigeon.php`: backend/app/Filament/Resources/PigeonResource/Pages - pigeon create page with single/range creation / 支持单个与范围创建的足环创建页面
- `EditPigeon.php`: backend/app/Filament/Resources/PigeonResource/Pages - pigeon edit page / 足环编辑页面
- `ListPigeons.php`: backend/app/Filament/Resources/PigeonResource/Pages - pigeon list page / 足环列表页面
