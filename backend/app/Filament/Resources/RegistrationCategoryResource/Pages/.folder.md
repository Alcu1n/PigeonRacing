# Folder: backend/app/Filament/Resources/RegistrationCategoryResource/Pages

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Progressive category pages expose list, create, edit, first-stage import, and member-scoped stage data routes. / 递进类别页面暴露列表、创建、编辑、第一阶段导入与按会员维护阶段数据路由。
Create/edit pages delegate schema to `RegistrationCategoryResource`; import owns upload state and confirmation tokens; stage management delegates writes to `AdminRegistrationEditService`. / 创建和编辑页面把结构委托给 `RegistrationCategoryResource`；导入页面负责上传状态与确认令牌；阶段管理通过 `AdminRegistrationEditService` 写入。
Import rows must be validated through `ProgressiveStageImportService`. / 导入行必须通过 `ProgressiveStageImportService` 校验。

## Files
- `CreateRegistrationCategory.php`: backend/app/Filament/Resources/RegistrationCategoryResource/Pages - progressive category create page / 递进类别创建页面
- `EditRegistrationCategory.php`: backend/app/Filament/Resources/RegistrationCategoryResource/Pages - progressive category edit page / 递进类别编辑页面
- `ImportFirstStage.php`: backend/app/Filament/Resources/RegistrationCategoryResource/Pages - first-stage Excel preview and commit page / 第一阶段 Excel 预览与确认写入页面
- `ListRegistrationCategories.php`: backend/app/Filament/Resources/RegistrationCategoryResource/Pages - progressive category list page / 递进类别列表页面
- `ManageStageData.php`: backend/app/Filament/Resources/RegistrationCategoryResource/Pages - member-scoped progressive stage data editor / 按会员维护递进阶段数据的编辑页面
