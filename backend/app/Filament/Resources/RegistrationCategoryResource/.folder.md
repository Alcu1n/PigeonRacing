# Folder: backend/app/Filament/Resources/RegistrationCategoryResource

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

This folder contains routed pages for progressive registration category configuration and stage data management. / 本文件夹包含递进报名类别配置与阶段数据管理的可路由页面。
It depends on `RegistrationCategoryResource` for race binding, current-stage selection, first-stage import entry, and stage data management entry. / 它依赖 `RegistrationCategoryResource` 提供赛事绑定、当前阶段选择、第一阶段导入入口与阶段数据管理入口。
Stage project definitions stay in `RaceProjectResource`; this resource selects the active stage and links to member-scoped stage data editing. / 阶段项目定义仍在 `RaceProjectResource`，本资源选择当前开放阶段并链接到按会员编辑阶段数据。

## Files
- `Pages/`: backend/app/Filament/Resources/RegistrationCategoryResource/Pages - progressive category resource page classes / 递进类别资源页面类
