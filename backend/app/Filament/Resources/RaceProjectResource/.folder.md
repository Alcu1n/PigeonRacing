# Folder: backend/app/Filament/Resources/RaceProjectResource

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

This folder contains routed pages for race project configuration. / 本文件夹包含报名项目配置的可路由页面。
It depends on `RaceProjectResource` for yuan-based table and form definitions. / 它依赖 `RaceProjectResource` 提供元单位表格与表单定义。
Project `group_size` remains the central rule dimension. / 项目 `group_size` 始终是核心规则维度。

## Files
- `Pages/`: backend/app/Filament/Resources/RaceProjectResource/Pages - race project resource page classes / 报名项目资源页面类
