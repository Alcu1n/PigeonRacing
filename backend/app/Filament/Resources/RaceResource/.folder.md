# Folder: backend/app/Filament/Resources/RaceResource

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

This folder contains routed pages for race administration. / 本文件夹包含赛事管理的可路由页面。
It depends on `RaceResource` for table and form definitions. / 它依赖 `RaceResource` 提供表格与表单定义。
Config version changes must remain visible to admins. / 配置版本变化必须对管理员可见。

## Files
- `Pages/`: backend/app/Filament/Resources/RaceResource/Pages - race resource page classes / 赛事资源页面类
