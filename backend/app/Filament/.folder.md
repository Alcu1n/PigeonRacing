# Folder: backend/app/Filament

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Filament owns administrator CRUD, review, and operational surfaces. / Filament 负责管理员 CRUD、审核与运营界面。
It edits configuration but must not bypass service-owned registration rules. / 它编辑配置，但不得绕过服务层报名规则。
Admin changes that affect member bootstrap must clear or version caches. / 影响会员初始化的后台变更必须清缓存或递增版本。

## Files
- `Resources/`: backend/app/Filament/Resources - admin resources for members, races, projects, pigeons, and registrations / 会员、赛事、项目、足环与报名后台资源
