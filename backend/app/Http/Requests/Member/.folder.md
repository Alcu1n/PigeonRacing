# Folder: backend/app/Http/Requests/Member

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Member requests validate login, password update, and normalized registration submission payloads. / 会员请求校验登录、改密与标准化报名提交数据。
Shape validation happens here; ownership validation happens in services or controllers. / 形状校验在这里；归属校验在服务层或控制器。
Submit payloads contain standard entries and optional progressive entries but no trusted amount. / 提交数据包含普通报名项与可选递进报名项，但不包含可信金额。

## Files
- `LoginRequest.php`: backend/app/Http/Requests/Member - login credential validation / 登录凭据校验
- `SubmitRegistrationRequest.php`: backend/app/Http/Requests/Member - registration submit payload validation / 报名提交数据校验
- `UpdatePasswordRequest.php`: backend/app/Http/Requests/Member - password-change payload validation / 改密数据校验
