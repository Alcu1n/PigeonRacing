# Folder: backend/app/Http/Requests

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Requests validate HTTP payload shape before services run. / 请求在服务运行前校验 HTTP 数据形状。
They do not enforce cross-model business rules. / 它们不执行跨模型业务规则。
Member request contracts live under the member subtree. / 会员请求契约位于会员子树。

## Files
- `Member/`: backend/app/Http/Requests/Member - member API request contracts / 会员 API 请求契约
