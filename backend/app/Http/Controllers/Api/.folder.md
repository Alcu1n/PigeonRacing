# Folder: backend/app/Http/Controllers/Api

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

API controllers expose JSON-only application boundaries. / API 控制器暴露仅 JSON 的应用边界。
They are grouped by caller role. / 它们按调用角色分组。
Member data isolation must be enforced before returning details. / 返回详情前必须强制会员数据隔离。

## Files
- `Member/`: backend/app/Http/Controllers/Api/Member - member-facing API controllers / 会员端 API 控制器
