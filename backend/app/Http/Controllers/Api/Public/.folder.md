# Folder: backend/app/Http/Controllers/Api/Public

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Public API controllers expose unauthenticated read-only JSON for website-style pages. / 公开 API 控制器为站点页面暴露无需登录的只读 JSON。
They must only return published content. / 它们只能返回已发布内容。
Member or admin-only data must stay outside this folder. / 会员或后台专属数据必须留在本文件夹之外。

## Files
- `InformationController.php`: backend/app/Http/Controllers/Api/Public - published information list and detail API / 已发布信息列表与详情 API
