# Folder: backend/app/Http/Middleware

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Middleware owns thin HTTP response policies that are not business services. / 中间件负责不属于业务服务的轻量 HTTP 响应策略。
It sits between routes/controllers and browser caches. / 它位于路由控制器与浏览器缓存之间。
Session-sensitive member APIs must not be cacheable. / 会话敏感的会员 API 绝不能被缓存。

## Files
- `NoStoreMemberApiResponse.php`: backend/app/Http/Middleware - no-store headers for member API JSON / 会员 API JSON 的 no-store 响应头
