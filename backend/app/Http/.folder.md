# Folder: backend/app/Http

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

HTTP code adapts requests into services, middleware, and serialized responses. / HTTP 代码把请求适配到服务、中间件与序列化响应。
It must not duplicate registration math or ownership rules. / 它不得重复报名金额或归属规则。
Request validation and middleware catch shape/session/cache hazards before services run. / 请求校验与中间件在服务运行前拦截数据形状、会话与缓存风险。

## Files
- `Controllers/`: backend/app/Http/Controllers - API controllers / API 控制器
- `Middleware/`: backend/app/Http/Middleware - session-sensitive API response middleware / 会话敏感 API 响应中间件
- `Requests/`: backend/app/Http/Requests - request validation contracts / 请求校验契约
