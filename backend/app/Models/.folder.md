# Folder: backend/app/Models

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Models map persistent business facts and relationships. / 模型映射持久化业务事实与关系。
They expose lifecycle helpers and cache invalidation only when the rule belongs to the aggregate. / 只有属于聚合本身的规则才暴露生命周期辅助方法与缓存失效。
Snapshots must preserve historical project, price, and ring-number truth. / 快照必须保留历史项目、价格与足环号码事实。

## Files
- `AdminLog.php`: backend/app/Models - admin audit log / 后台审计日志
- `ImportBatch.php`: backend/app/Models - Excel import batch counters / Excel 导入批次计数
- `Member.php`: backend/app/Models - member identity aggregate with optional imported login credentials / 支持导入时可空登录凭据的会员身份聚合
- `Pigeon.php`: backend/app/Models - pigeon ring model and owner cache invalidation / 足环模型与归属缓存失效
- `Race.php`: backend/app/Models - race aggregate and open-state helper / 赛事聚合与开放状态辅助
- `RaceProject.php`: backend/app/Models - configurable project rule model with race cache invalidation / 带赛事缓存失效的可配置项目规则模型
- `Registration.php`: backend/app/Models - registration aggregate / 报名聚合
- `RegistrationEntry.php`: backend/app/Models - registration project snapshot / 报名项目快照
- `RegistrationEntryPigeon.php`: backend/app/Models - entry pigeon snapshot / 报名足环快照
- `User.php`: backend/app/Models - admin user / 后台管理员
