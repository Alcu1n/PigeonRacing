# Folder: backend/app/Models

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Models map persistent business facts, progressive categories, public information posts, global settings, and relationships. / 模型映射持久化业务事实、递进类别、公开信息发布、全局设置与关系。
They expose lifecycle helpers and cache invalidation only when the rule belongs to the aggregate. / 只有属于聚合本身的规则才暴露生命周期辅助方法与缓存失效。
Snapshots must preserve historical project, price, stage, and ring-number truth. / 快照必须保留历史项目、价格、阶段与足环号码事实。

## Files
- `AdminLog.php`: backend/app/Models - admin audit log / 后台审计日志
- `AppSetting.php`: backend/app/Models - global key-value settings / 全局键值设置
- `ImportBatch.php`: backend/app/Models - Excel import batch counters / Excel 导入批次计数
- `InformationPost.php`: backend/app/Models - public information publishing aggregate with category/status helpers / 带分类状态辅助方法的公开信息发布聚合
- `Member.php`: backend/app/Models - member identity aggregate with optional imported login credentials and password-change policy / 支持导入时可空登录凭据与改密策略的会员身份聚合
- `Pigeon.php`: backend/app/Models - pigeon ring model and owner cache invalidation / 足环模型与归属缓存失效
- `ProgressiveStageEntry.php`: backend/app/Models - progressive stage membership snapshot and confirmation state / 递进阶段足环快照与确认状态
- `Race.php`: backend/app/Models - race aggregate and open-state helper / 赛事聚合与开放状态辅助
- `RaceProject.php`: backend/app/Models - configurable project rule model with race cache invalidation / 带赛事缓存失效的可配置项目规则模型
- `RegistrationCategory.php`: backend/app/Models - progressive registration category aggregate and current-stage pointer / 递进报名类别聚合与当前阶段指针
- `Registration.php`: backend/app/Models - registration aggregate with localized confirmation status helpers / 带本地化确认状态辅助方法的报名聚合
- `RegistrationEntry.php`: backend/app/Models - registration project snapshot / 报名项目快照
- `RegistrationEntryPigeon.php`: backend/app/Models - entry pigeon snapshot / 报名足环快照
- `User.php`: backend/app/Models - admin user / 后台管理员
