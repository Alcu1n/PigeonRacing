# Folder: backend/app/Enums

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Enums centralize lifecycle string values used by models and APIs. / 枚举集中模型与 API 使用的生命周期字符串。
They prevent duplicated magic status strings in services. / 它们防止服务中重复魔法状态字符串。
Database values must match enum backing values. / 数据库值必须匹配枚举底层值。

## Files
- `RaceStatus.php`: backend/app/Enums - race lifecycle values / 赛事生命周期值
- `RegistrationStatus.php`: backend/app/Enums - registration lifecycle values / 报名生命周期值
