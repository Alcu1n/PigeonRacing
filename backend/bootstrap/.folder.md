# Folder: backend/bootstrap

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Bootstrap wires Laravel application creation, middleware, proxy trust, and provider discovery. / 启动目录连接 Laravel 应用创建、中间件、代理信任与服务提供者发现。
It must remain framework-level glue, not business logic. / 它必须保持框架胶水层，而不是业务逻辑。
Provider registry must include the admin panel provider; proxy metadata must preserve HTTPS asset URLs. / 服务提供者注册表必须包含后台面板提供者；代理元数据必须保持 HTTPS 资源链接。

## Files
- `app.php`: backend/bootstrap - Laravel application bootstrap / Laravel 应用启动
- `providers.php`: backend/bootstrap - service provider list / 服务提供者列表
