# Folder: backend/tests/Unit

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Unit tests exercise pure service behavior. / 单元测试验证纯服务行为。
They should fail on bad amount, group-size, ownership, duplicate groups, repeat, or usage-limit rules. / 金额、羽数、归属、重复组合、重复或次数限制规则错误时应失败。
They avoid database dependency unless behavior requires persistence. / 除非行为需要持久化，否则避免数据库依赖。

## Files
- `RegistrationSubmissionServiceTest.php`: backend/tests/Unit - registration rule and duplicate-group tests / 报名规则与重复组合测试
- `RingNumberRangeTest.php`: backend/tests/Unit - prefix/full ring-number parsing, leading zeroes, and inclusive counting / 前缀或完整足环号码解析、前导零与闭区间计数
