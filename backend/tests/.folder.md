# Folder: backend/tests

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Tests verify backend business rules, cache behavior, and API behavior. / 测试验证后端业务规则、缓存行为与 API 行为。
Unit tests cover rule services without HTTP. / 单元测试不经 HTTP 覆盖规则服务。
Feature tests cover persistence behavior that needs Laravel runtime. / 功能测试覆盖需要 Laravel 运行时的持久化行为。

## Files
- `Unit/`: backend/tests/Unit - backend unit tests / 后端单元测试
- `Feature/`: backend/tests/Feature - Laravel API feature tests / Laravel API 功能测试
- `TestCase.php`: backend/tests - Laravel application test base / Laravel 应用测试基类
