# Folder: backend/routes

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Routes expose member API, web entry, and console scheduling boundaries. / 路由暴露会员 API、Web 入口与控制台调度边界。
Member API routes use web sessions for Sanctum cookie auth. / 会员 API 路由使用 Web 会话支撑 Sanctum Cookie 鉴权。
No route should perform business validation inline. / 任何路由都不应内联执行业务校验。

## Files
- `api.php`: backend/routes - member API routes / 会员 API 路由
- `console.php`: backend/routes - scheduler definitions / 调度定义
- `web.php`: backend/routes - web routes / Web 路由
