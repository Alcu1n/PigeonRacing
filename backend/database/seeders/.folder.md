# Folder: backend/database/seeders

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Seeders create reproducible local demo data. / 种子创建可复现本地演示数据。
They must be safe to run repeatedly. / 它们必须可重复运行。
Demo projects must exercise both single and multi group logic. / 演示项目必须覆盖单羽与多羽逻辑。

## Files
- `DatabaseSeeder.php`: backend/database/seeders - demo admin, member, race, project, and pigeon data / 演示管理员、会员、赛事、项目与足环数据
