# 赛鸽赛事报名系统上下文

本上下文记录赛事报名场景中的关键业务术语与容量目标，避免把用户人数、请求速率和静态资源访问混为一谈。

## 访问目标

**200人峰值访问**：200个用户在60秒内进入会员端、完成登录、查看赛事并打开报名页面；另行验证20–30个用户同时提交报名。
_Avoid_: 200 RPS、200个并发HTTP连接

**静态资源源站**：在 CDN 未命中时，为会员端构建出的静态文件提供权威读取位置，并与承载页面和 API 的应用源站分离。
_Avoid_: 将会员端静态资源和报名 API 视为同一个容量边界
