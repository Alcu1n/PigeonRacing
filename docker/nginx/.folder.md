# Folder: docker/nginx

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

Nginx owns static frontend delivery, Laravel reverse proxy routing, and large upload body limits. / Nginx 负责前端静态交付、Laravel 反向代理路由与大上传请求体限制。
`/api`, `/admin`, `/sanctum`, `/storage`, and Filament/Livewire assets go to backend PHP. / `/api`、`/admin`、`/sanctum`、`/storage` 与 Filament/Livewire 资源进入后端 PHP。
Hashed assets may be cached long-term; HTML must stay fresh. / 带 hash 的资源可长期缓存；HTML 必须保持新鲜。

## Files
- `default.conf`: docker/nginx - server routing, cache rules, and 64MB upload body limit / 服务路由、缓存规则与 64MB 上传请求体限制
