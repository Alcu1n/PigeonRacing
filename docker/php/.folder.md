# Folder: docker/php

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时，更新此文档。

PHP runtime builds Laravel dependencies, required extensions, and large-import limits. / PHP 运行时构建 Laravel 依赖、所需扩展与大批量导入限制。
It serves only backend PHP-FPM traffic; queues use the same image. / 它只服务后端 PHP-FPM 流量；队列使用同一镜像。
Production images must run Composer install before PHP-FPM starts. / 生产镜像必须在 PHP-FPM 启动前完成 Composer 安装。

## Files
- `Dockerfile`: docker/php - PHP-FPM runtime image with upload override copy / 带上传覆盖配置复制的 PHP-FPM 运行镜像
- `uploads.ini`: docker/php - PHP upload, memory, and execution limits for large Excel imports / 大 Excel 导入的 PHP 上传、内存与执行时间限制
